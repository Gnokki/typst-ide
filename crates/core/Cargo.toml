[package]
name = "core"
version = "0.1.0"
edition = "2024"

[dependencies]
typst = "*"
typst-as-library = { path = "../typst-as-library" }
typst-pdf = "*"
typst-svg = "*"

[dev-dependencies]
tempfile = "3.0.0"
pdf-extract = "0.10.0"